tailwind.config = {
    darkMode: 'class', // 👈 Crucial context wrapper for element toggles
    theme: {
        extend: {
            fontFamily: {
                sans: ['Inter', 'sans-serif'],
            },
        },
    },
}